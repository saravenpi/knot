# Database Configuration
POSTGRES_PASSWORD=your_secure_database_password_here

# JWT Configuration (CRITICAL: Change these in production!)
JWT_SECRET=your-super-secure-jwt-secret-minimum-32-characters-long
ROCKET_SECRET_KEY=your-rocket-secret-key-for-cookies-and-sessions

# Optional: Custom database URL (overrides default)
# DATABASE_URL=postgresql://knot:password@localhost:5432/knot_space

# Optional: Knot Space API URL for CLI
# KNOT_SPACE_URL=https://api.knot.space

# Optional: SSL Certificate paths for production
# SSL_CERT_PATH=/path/to/fullchain.pem
# SSL_KEY_PATH=/path/to/privkey.pem

# Optional: Custom storage path
# STORAGE_PATH=/var/lib/knot/storage

# Optional: Custom log level
# RUST_LOG=info